spring.application.name=library-management

# Use a different port when 8080 is occupied locally
server.port=${SERVER_PORT:8081}

# ================= EMAIL CONFIG =================
# Read from environment variables for security (production)
# Fallback to default values for local development
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:parshuram8792@gmail.com}
spring.mail.password=${MAIL_PASSWORD:qihk bypu omcx dioz}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
